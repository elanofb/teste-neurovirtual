@model WEB.Areas.Associados.ViewModels.AssociadoForm

<div class="tab-pane" id="tab-representantes">
    <div id="boxLoadFormAssociadoRepresentante" class="content-load carregando" data-url="@Url.Action("editar", "associadorepresentante", new {area = "associados", idAssociado = Model.Associado.id})"></div>
    <br/>
    <h4 class="separator-form">Sócios e Representantes</h4>
    <div id="boxLoadListaAssociadoRepresentante" class="content-load carregando" data-url="@Url.Action("partial-listar-representantes", "associadorepresentante", new {area = "associados", idAssociado = Model.Associado.id })"></div>
</div>