@model WEB.Areas.Associados.ViewModels.AssociadoForm
@if (Model.Associado.id > 0) {

    <div class="form-horizontal info-cadastro">

        <div class="col-md-3 col-sm-12 col-lg-3">
            <div class="row">
                <div class="col-md-6 col-sm-4">
                    <label>ID</label>
                    <input type="text" class="form-control input-sm" readonly="readonly" value="@Model.Associado.id" />
                </div>
                @if (Model.Associado.nroAssociado > 0) {
                    <div class="col-md-6 col-sm-4">
                        <label>Nº</label>
                        <input type="text" class="form-control input-sm" readonly="readonly" value="@Model.Associado.nroAssociado" />
                    </div>
                }
            </div>
        </div>
        <div class="col-md-6 col-sm-12 col-lg-6">
            <div class="row">
                <div class="col-md-4 col-sm-4 ">
                    <label>Cadastro</label>
                    <input type="text" class="form-control input-sm text-center" readonly="readonly" value="@Model.Associado.dtCadastro.exibirData(false)" />
                </div>
                <div class="col-md-4 col-sm-4  no-padding-left">
                    <label>Alteração</label>
                    <input type="text" class="form-control input-sm text-center" readonly="readonly" value="@Model.Associado.dtAlteracao.exibirData(true)" />
                </div>
                <div class="col-md-4 col-sm-4  no-padding-left">
                    <label>Admissão</label>
                    <input type="text" class="form-control input-sm text-center" readonly="readonly" value="@Model.Associado.dtAdmissao.exibirData()" />
                </div>
            </div>
        </div>

        <div class="col-md-3 col-sm-12 col-lg-3 no-padding-right">
            <div class="row margin-top-25">
                <div class="col-md-6 col-xs-6 no-padding-right">
                    @exibirLabelStatus()
                </div>
                <div class="col-md-6 col-xs-6">
                    @exibirSituacaoFinanceira()
                </div>
            </div>
        </div>

    </div>
    <div class="clearfix"></div><br />
}

@helper exibirLabelStatus() {

    if (Model.Associado.ativo == "S") {
            <a class="bg-green label-info text-center">ATIVO</a>
    } else if (Model.Associado.ativo == "E") {
            <a class="bg-yellow label-info text-center">EM ADMISSÃO</a>
    } else {
            <a class="bg-red label-info text-center">DESATIVADO</a>
    }
}

@helper exibirSituacaoFinanceira() {



}