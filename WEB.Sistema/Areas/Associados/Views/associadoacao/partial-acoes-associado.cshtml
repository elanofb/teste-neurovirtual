@model DAL.Associados.Associado

@if (Model.id > 0) {
	<div>
		@if (Model.ativo == "E" || !Model.dtAdmissao.HasValue) {
			<button class="btn btn-success btn-lg col-md-12" onclick="DefaultSistema.showModal('@Url.Action("partial-admissao-associado", "associadoacao", new{ area = "associados", idAssociado = Model.id })');">
				<i class="fa fa-check"></i> Admitir Associado
			</button>
			<div class="clearfix"></div><br />
		}

		@*@if (Model.ativo == "S" || Model.ativo == "E") {
			<button class="btn btn-danger btn-lg col-md-12" onclick="DefaultSistema.showModal('@Url.Action("partial-desativacao-associado", "associadoacao", new{ area = "associados", idAssociado = Model.id })');">
				<i class="fa fa-remove"></i> Desativar Associado
			</button>
			<div class="clearfix"></div><br />
		}

		@if (Model.ativo == "N" || Model.ativo == null) {
			<button class="btn btn-warning btn-lg col-md-12" onclick="DefaultSistema.showModal('@Url.Action("partial-reativacao-associado", "associadoacao", new{ area = "associados", idAssociado = Model.id })');">
				<i class="fa fa-edit"></i> Reativar Associado
			</button>
			<div class="clearfix"></div><br />
		}

		<button class="btn btn-danger btn-lg col-md-12" onclick="DefaultSistema.showModal('@Url.Action("partial-excluir-associado", "associadoacao", new{ area = "associados", idAssociado = Model.id })');">
			<i class="fa fa-edit"></i> Excluir Cadastro
		</button>*@
        <div class="clearfix"></div><br />

        <button class="btn btn-primary btn-lg col-md-12" onclick="DefaultSistema.showModal('@Url.Action("modal-envio-cadastro-por-email", "associadoacao", new { Area = "Associados", idAssociado = Model.id })');">
			<i class="fa fa-envelope"></i> Enviar Cadastro por E-mail
		</button>
		<div class="clearfix"></div><br />

	</div>

}